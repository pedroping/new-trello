/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DarkModeToggleDirective } from './dark-mode-toggle.directive';
describe('Directive: GenerateIcon', () => {
  it('should create an instance', () => {
    const directive = new DarkModeToggleDirective();
    expect(directive).toBeTruthy();
  });
});
