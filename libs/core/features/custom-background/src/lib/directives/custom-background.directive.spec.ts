/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CustomBackgroundDirective } from './custom-background.directive';

describe('Directive: CustomBackground', () => {
  it('should create an instance', () => {
    const directive = new CustomBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
