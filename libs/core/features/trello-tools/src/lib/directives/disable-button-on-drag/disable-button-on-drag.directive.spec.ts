/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DisableButtonOnDragDirective } from './disable-button-on-drag.directive';

describe('Directive: DisableButtonOnDrag', () => {
  it('should create an instance', () => {
    const directive = new DisableButtonOnDragDirective();
    expect(directive).toBeTruthy();
  });
});
