/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PreviewCardHeightDirective } from './preview-card-height.directive';

describe('Directive: PreviewCardHeight', () => {
  it('should create an instance', () => {
    const directive = new PreviewCardHeightDirective();
    expect(directive).toBeTruthy();
  });
});
