/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BackdropContentDirective } from './backdrop-content.directive';

describe('Directive: BackdropContent', () => {
  it('should create an instance', () => {
    const directive = new BackdropContentDirective();
    expect(directive).toBeTruthy();
  });
});
