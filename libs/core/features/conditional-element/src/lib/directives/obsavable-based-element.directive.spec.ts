/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ObsavableBasedElementDirective } from './obsavable-based-element.directive';

describe('Directive: ObsavableBasedElement', () => {
  it('should create an instance', () => {
    const directive = new ObsavableBasedElementDirective();
    expect(directive).toBeTruthy();
  });
});
